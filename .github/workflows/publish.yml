name: "🚀 update"

on:
    push:
      branches: [ master ]

jobs:
    release:
        name: 🚀 publish
        runs-on: ubuntu-latest
        steps:
            - name: 📚 checkout
              uses: actions/checkout@v2.1.1
              with:
                  node-version: "16.6.2"
                  registry-url: https://registry.npmjs.org
            - name: 🚀 publish
              run: npm publish --access public
              env:
                  NODE_AUTH_TOKEN: ${{env.NPM_AUTH_TOKEN.NPM_AUTH_TOKEN}}